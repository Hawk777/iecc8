namespace Iecc8.World {
	/// <summary>
	/// The colour and flash pattern of a single head of a signal.
	/// </summary>
	public enum EAspect {
		Red,
		Green,
		Yellow,
		FlashingYellow,
		Lunar,
	}
}
